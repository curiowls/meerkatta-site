# Paired Lock Screen demos
Autonomous Shotcraft production. Two real UI tutorials embedded side by side: setup then capture. User supplied exact footage and approved layout, so a separate styleframe is unnecessary. Website cream/charcoal/brown styling, matching crop, no baked outer frame or captions. All instructional captions below videos. Silent embedded tutorials: promotional BGM, SFX and elaborate end-card rules are inapplicable.

Reference breakdown: app preview uses real footage, widget focus, natural-speed speaking, shortened processing, readable final hold. Retain actual footage and holds; remove promotional title canvas and frame. No Gallery recipe copied: custom editorial cuts preserve real operations without unrelated animated UI. Verify cut continuity, complete steps, final holds and caption alignment.

Setup storyboard at30fps: source0–4.1s (123f) long press/customize/widget area;6–7.8s played at 3× (18f) with app-list blur;8.8–13.2s(132f) select MeerKatta/add widget;14–16s(60f) Done;17.8–19s(36f) installed; final still45f. Total414f/13.8s. Suggested widgets with unrelated personal data and redundant overscroll/saving wait are omitted.

Capture storyboard: lockscreen still30f; opening60f; speech150f; processing60f; result90f. Total390f/13s. Uses original sources behind the approved app preview, no promotional frame. Dates, times and status values are softened with feathered spatial blur. Editing controls and widgets remain visible; the wallpaper picker uses a separate inset clock mask. No replacement dates are fabricated.

Outputs540×1170/30fps/H.264/yuv420p. Native UI stays intact; no captions baked in. Website phone max244px,16% below previous290, height adapts to notebook viewport. Exclusive sequential playback; offscreen pause, manual controls, reduced-motion handling. Below-video captions and transcripts provide readable instructions.

Attention: the second phone uses a static cream Up next cover until capture starts. Replaying setup restores the cover. Captions stay below both videos.
